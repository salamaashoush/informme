type Query{
    metes: [Meta!]! @paginate(defaultCount:10) 
    meta(id:ID @eq):Meta @find
}

type Meta{
    id: ID!
    key:String!
    value:String!
    metable: Metable! @morphTo
}
union Metable = Center|Meta
union Metable = Place|Meta
union Metable = Service|Meta
union Metable = User|Meta